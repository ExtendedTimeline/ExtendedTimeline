ET_IndochinaWar_decisions = {
	ET_IndochinaWar_Recognize_Vietnam = {
		icon = generic_political_discourse

		is_good = yes

		available = {
            has_war = no
			OR = {
				original_tag = SOV
				original_tag = PRC
			}
			OR = {
				NOT = { has_country_flag = ET_flag_country_IndochinaWar_Recognize_Vietnam_SOV }
				NOT = { has_country_flag = ET_flag_country_IndochinaWar_Recognize_Vietnam_PRC }
			}
		}

		visible = {
			OR = {
				original_tag = SOV
				original_tag = PRC
			}
		}

		fire_only_once = no
		cost = 250

		complete_effect = {
			if = {
				limit = { original_tag = SOV }
				set_country_flag = ET_flag_country_IndochinaWar_Recognize_Vietnam_SOV
				set_global_flag = ET_flag_global_IndochinaWar_Recognize_Vietnam_SOV
			}
			if = {
				limit = { original_tag = CHI }
				set_country_flag = ET_flag_country_IndochinaWar_Recognize_Vietnam_CHI
				set_global_flag = ET_flag_global_IndochinaWar_Recognize_Vietnam_CHI
			}
		}

		ai_will_do = {
			base = 100
		}
	}

	# Colonial Master can release Laos And Cambodia
	ET_IndochinaWar_puppet_laos = {
		icon = generic_decision

		is_good = yes

		available = {
			VIN = { has_war_with = ROOT }
			has_full_control_of_state = 670
			OR = {
				date > 1953.10.22
				has_country_flag = ET_Flag_Global_DEBUG_Decolonization_Ignore_Dates
			}
		}

		visible = {
			VIN = { has_war_with = ROOT }
			date > 1950.10.22
		}

		fire_only_once = no
		cost = 0

		complete_effect = {
			release_puppet = LAO
		}

		ai_will_do = {
			base = 100
		}
	}
	ET_IndochinaWar_puppet_cambodia = {
		icon = generic_decision

		is_good = yes

		available = {
			VIN = { has_war_with = ROOT }
			has_full_control_of_state = 670
			OR = {
				date > 1953.11.09
				has_country_flag = ET_Flag_Global_DEBUG_Decolonization_Ignore_Dates
			}
		}

		visible = {
			VIN = { has_war_with = ROOT }
			date > 1950.11.09
		}

		fire_only_once = no
		cost = 0

		complete_effect = {
			release_puppet = CAM
		}

		ai_will_do = {
			base = 100
		}
	}
}